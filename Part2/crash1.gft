//The program can be crashed, if we modify the `setupgc()` function as follows:

void setupgc() {
    
    examplegcd.gift_card_record_data = malloc(examplegcd.number_of_gift_card_records * sizeof(struct gift_card_record_data));

}
